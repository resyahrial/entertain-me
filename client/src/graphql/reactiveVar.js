import { makeVar } from "@apollo/client";

export const showBottomSheet = makeVar(false);
export const detailId = makeVar(null);
export const formState = makeVar(false);
export const showItem = makeVar("movies");
export const reFetch = makeVar(false);
export const favouritesData = makeVar([]);
